name: bot_detection
channels:
  - nvidia
  - pytorch
  - pyg
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pytorch=2.4.0
  - torchvision=0.19.0
  - torchaudio=2.4.0
  - pytorch-cuda=12.4           # GPU acceleration, might need to change based on your GPU
  - pyg                         # meta-package installs torch-geometric + extensions
  - networkx
  - pandas
  - numpy
  - scikit-learn
  - matplotlib
  - tqdm
  - ipykernel
  - duckdb
  - pyarrow
  - pip

### I had to install these individually (link needs to change if you use a different pytorch version):
# pip install torch-scatter torch-sparse torch-geometric -f https://data.pyg.org/whl/torch-2.4.0+cu124.html
# pip install torch-geometric-temporal
